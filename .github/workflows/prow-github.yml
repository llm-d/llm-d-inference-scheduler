name: Prow Commands
on:
  issue_comment:
    types: [created]
jobs:
  prow:
    uses: llm-d/llm-d-infra/.github/workflows/reusable-prow-commands.yml@main
    permissions:
      issues: write
      pull-requests: write
