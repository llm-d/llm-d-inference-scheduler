{{- define "inference-extension.sa-token-secret" -}}
{{- $monitoringProviderName := include "llm-d-inference-scheduler.monitoringProviderName" . | lower -}}
{{- if and .Values.inferenceExtension.monitoring.prometheus.enabled .Values.inferenceExtension.monitoring.prometheus.auth.enabled (ne $monitoringProviderName "gke") }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.inferenceExtension.monitoring.prometheus.auth.secretName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "llm-d-inference-scheduler.labels" . | nindent 4 }}
  annotations:
    kubernetes.io/service-account.name: {{ include "llm-d-inference-scheduler.name" . }}
type: kubernetes.io/service-account-token
---
{{- end }}
{{- end }}
